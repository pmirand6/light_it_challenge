# Here you can define shortcuts and aliases to common tasks (commands)
# you will run in your local environment or CI or deploy.
#  Use the scripts defined below with:
#    $ kool run <script>
# Learn more at: https://kool.dev/docs/getting-started/how-it-works#koolyml
scripts:
  composer: kool exec app composer
  artisan: kool exec app php artisan
  phpunit: kool exec app php ./bin/phpunit
  setup:
    - kool run before-start
    - kool start
    - kool run composer install
    - kool run artisan key:generate
    - kool run create-db-if-not-exists # New script to create the database if not exists
  reset:
    - kool run composer install
    - kool run artisan migrate:fresh --seed
  before-start:
    - kool docker kooldev/bash -c "cp .env.example .env"
  # CLI access to MySQL
  mysql: kool exec -e MYSQL_PWD=secret database mysql -u $DB_USERNAME $DB_DATABASE

  # New script to create the database if not exists
  create-db-if-not-exists: kool exec -e MYSQL_PWD=secret database mysql -uroot -e "CREATE DATABASE IF NOT EXISTS $DB_DATABASE;"
